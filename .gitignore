# Rust build artifacts
/target

# ROM files (legal reasons — add your own)
/roms/*.rom
/roms/*.gb


# WASM build output
/web/pkg

# Debug artifacts
/debugging
build_log.txt
/games/*.gb
/web/compiler_pkg/
